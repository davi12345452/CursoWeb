/*
  Criando um programa de preço e desconto somente com variáveis
*/

let preco = 40; // Valor do produto = 40
let desconto = 0.2; // Desconto = 20%
let p_final = false; // Variável vazia

p_final = 40 * (1-desconto); // Aplicando o desconto
console.log(p_final); // Imprimindo o preço com desconto